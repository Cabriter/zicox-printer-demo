apply plugin: 'com.android.application'

android {
    compileSdkVersion 8
    buildToolsVersion "29.0.0"

    defaultConfig {
        applicationId "com.example.demo_ad_sdk"
        minSdkVersion 7
        targetSdkVersion 16
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
    implementation fileTree(dir: '/Users/cabrite/Desktop/WorkSpace/zicox-printer-demo/libs', include: ['*.aar', '*.jar'], exclude: [])
}
